# nested code

nested_compound_expression == {

    nothing : {
        to : {
            see: {
                here: {
                    while depth > 1 do unnest() end ;

                    fun1(fun2(fun3(fun4())))

                    : (((ok)) & (done)) | over
                }
            }
        }
    }
}