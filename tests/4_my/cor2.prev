# check scopes
{
    i % j
    :
    {
        j - i  : k
        where
        var i: ptr int;
        var j: ptr boo;
        typ boo: bool;
        var k : int

    }
    where
    var i: char;
    var j: bool;
    fun f(i:int) : int = g(i*2) ;
    fun g(j:int) : int = f(j/2)
}