
# function greatest common divisor
# I actually have no idea if this is the correct syntax
fun integer gcd(integer a, integer b) { #define function that return integer
										#and takes two integers: a and b
	if a==0 then
		b #i am making an assumption that this language is functional
	else
		gcd (b%a, a)
	end
}

fun integer main(string arr args){
	printf('a', gcd(256,198))
}
